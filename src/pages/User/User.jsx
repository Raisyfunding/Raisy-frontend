import { Image } from '@chakra-ui/react'
import React from 'react'
import { Screen } from '../../styles/globalStyles'

const User = () => {
  return (
    <Screen>
      <Image width={'100vw'} position={'absolute'} top={'0'} />
    </Screen>
  )
}

export default User
